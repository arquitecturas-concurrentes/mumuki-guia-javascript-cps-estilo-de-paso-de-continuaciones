describe("successor", function() {
  it("successor(1) == 2", function() {
    assert.equal(successor(1), 2)
  })
  
  it("successor(30) == 31", function() {
    assert.equal(successor(30), 31)
  })
})